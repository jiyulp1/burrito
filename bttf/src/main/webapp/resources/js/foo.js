/**
 	$(document).ready(function() {
 	    $('#foo-table').DataTable( {
 	    	bInfo : false,
 	       	bPaginate : true,
 	        displayLength : 10
 	    } );
 	} );	
 */

 	$(document).ready(function() {
 	    $('#foo-table').DataTable( {
 	    	bInfo : false,
// 	    	bSortable : true,
 	       	bPaginate : true,
 	        displayLength : 10
 	    } );
 	    
 	   $('#foo-table1').DataTable( {
	    	bInfo : false,
//	    	bSortable : true,
	       	bPaginate : true,
	        displayLength : 10
	    } );
 	} );	
