<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.bttf.mappers.board">
	<!-- 1. 게시물 목록 -->
	<select id="csslist" resultType="kr.co.bttf.domain.CssBoardVO">
		select * from css
	</select>
	
	
	<!-- 2. 게시물 작성 -->
	<insert id="csswrite" parameterType="kr.co.bttf.domain.CssBoardVO">
		insert into css (post_subject, post_content, writer)
		values(#{post_subject}, #{post_content}, #{writers})
	</insert>
	
	
	<!-- 3. 게시물 조회 -->
	<select id="cssview" parameterType="int" resultType="kr.co.bttf.domain.CssBoardVO">
		select * from css
		where post_id = #{post_id}
	</select>
	
	
	<!-- 4. 게시물 수정 -->
	<update id="cssmodify" parameterType="kr.co.bttf.domain.CssBoardVO">
		update css set
		post_subject = #{post_subject}, post_content = #{post_content}, writer = #{writer}
		where post_id = #{post_id}
	</update>
	
	
	<!--5. 게시물 삭제 -->
	<delete id="cssdelete" parameterType="int">
		delete from css
		where post_id = #{post_id}
	</delete>

</mapper>